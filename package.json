{
  "name": "codeplane",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "dev": "bun run dev.ts",
    "start": "bun run packages/server/src/index.ts",
    "test": "bun test",
    "db:push": "source .env && cd packages/server && bunx drizzle-kit push"
  },
  "devDependencies": {
    "@anthropic-ai/sdk": "^0.78.0"
  }
}
