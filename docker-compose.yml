services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: codeplane
      POSTGRES_PASSWORD: codeplane
      POSTGRES_DB: codeplane
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
